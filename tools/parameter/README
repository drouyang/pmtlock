A kernel module used to inspect and tune kernel varibles

Usage:
1. Modify pmtlock_debugfs.c, add variables you want to inspect. Make sure the variable is exported.
2. make
3. read from debugfs /sys/kernel/debug/pmtlock/
